<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>APIVerve.API.LanguageDetector</Title>
    <Version>1.0.0</Version>
    <Authors>APIVerve</Authors>
    <Company>APIVerve</Company>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>False</PackageRequireLicenseAcceptance>
    <PackageReadmeFile>Readme.md</PackageReadmeFile>
    <PackageProjectUrl>https://apiverve.com/marketplace/api/languagedetector</PackageProjectUrl>
    <SignAssembly>False</SignAssembly>
    <Description>Language Detector is a simple tool for detecting the language of a text. It returns the language code and the confidence level.</Description>
    <RepositoryUrl>https://github.com/apiverve/languagedetector-NET</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>language detector; language detection; language detector api; language detection api; language detector tool; language detection tool; language detector software; language detection software; language detector service; language detection service; language detector library; language detection library; language detector sdk; language detection sdk; language detector code; language detection code; language detector script; language detection script; language detector program; language detection program; language detector application; language detection application; language detector app; language detection app; language detector utility; language detection utility; language detector helper; language detection helper; language detector helper tool; language detection helper tool; language detector helper software; language detection helper software; language detector helper service; language detection helper service; language detector helper library; language detection helper library; language detector helper sdk; language detection helper sdk; language detector helper code; language detection helper code; language detector helper script; language detection helper script; language detector helper program; language detection helper program; language detector helper application; language detection helper application; language detector helper app; language detection helper app; language detector helper tool; language detection helper tool; language detector helper utility; language detection helper utility; language detector helper</PackageTags>
    <Copyright>©2024 APIVerve. All Rights Reserved</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <None Update="LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Readme.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

</Project>
